<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
					<a class="navbar-brand logo" href="/public"></a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">
				<li><a href="/public">Start</a></li>
				<li><a href="/public/articles">Artykuły</a></li>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">Galerie<b class="caret"></b></a>
					<ul class="dropdown-menu">
						<% gallery_menu = Gallery.where(:visible => true).last(10) %>
						<% gallery_menu.each do |gallery| %>
						<li><%= link_to(gallery.name, {:action => 'galleries', :gallery_id => gallery.id}) %> </li>
						<% end %>
					</ul>
				</li>
				<li class="dropdown">
					<a href="#" class="dropdown-toggle" data-toggle="dropdown">Kategorie <b class="caret"></b></a>
					<ul class="dropdown-menu">
						<% category_menu = Category.where(:visible => true).last(10) %>
						<% category_menu.each do |category| %>
						<li><%= link_to(category.name, {:action => 'categories', :category_id => category.id}) %> </li>
						<% end %>
					</ul>
				</li>
				<li><a href="/admin">Panel administracjny</a></li>
			</ul>
    </div>
  </div>
</nav>
