<% @page_title = @page.name %>
<section id="content">
    <div class="container">
<div class="row">
<div class="col-lg-12">
	<h2>Strona: <%= @page.name %></h2>
</div>
</div>
            <% @page.articles.visible.sort.each do |article| %>
<div class="row article">
<div class="col-lg-12">
           <h3><%= article.name %></h3>
<hr />
</div>

		 <div class="col-lg-4 col-lg-offest-0 col-md-4 col-md-offset-0 col-sm-8 col-sm-offset-2 col-xs-8 col-xs-offset-2">
                <%= image_tag(article.photo.url, :class => "img-responsive img-thumbnail") %>
            </div>
            <div class="col-lg-8 col-md-8 col-sm-12 col-xs-12">
                <p><%= raw(article.content) %></p>
            </div>
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            
            </div>
</div>
            <% end %>
<div class="row">
            <div class="col-lg-12">
                <%= link_to("powrót", {:controller => 'public', :action => 'categories', :category_id => @page.category.id}, :class => 'btn btn-info') %>
            </div>
</div>
    </div>
</section>
